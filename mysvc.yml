apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
  labels:
    app: myapp-pod
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30002
    protocol: TCP
  selector:
    app: myapp-pod
