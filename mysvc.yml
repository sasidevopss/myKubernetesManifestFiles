apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
  labels:
    app: myapp-pod
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30001
    protocol: TCP
  selector:
    app: myapp-pod
